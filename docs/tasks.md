# tasks.md - FitLoop

> 把“先做什么、后做什么”说清楚：30-60 分钟小任务 | 依赖顺序 | 完成信号

---

## 一、任务拆分原则

- 每个任务控制在 30-60 分钟内完成。
- 任务按依赖顺序推进，前置任务没有完成时不开始后置任务。
- 每个任务完成后都要能看到明确结果，例如文件生成、页面可见、数据可保存、错误可提示。
- 遵守 `plan -> code -> review`：每次进入新任务前先确认目标，完成后做最小验证和自查。
- 代码文件超过 200 行时必须评估拆分，优先拆到 `components/`、`utils/` 或 `api/`。
- 每次代码更新后同步维护 `README.md` 和 `ARCHITECTURE.md`。

---

## 二、总体依赖顺序

```
项目骨架
  -> 基础工具与默认数据
  -> 4 Tab 页面框架
  -> 档案录入
  -> 训练计划展示
  -> 训练计划编辑
  -> 今日日志
  -> Prompt 构建
  -> AI 对话
  -> JSON 建议解析
  -> 一键采纳写回
  -> 验证与演示材料
```

核心 demo 必须跑通：

```
填写档案 -> 设置训练计划 -> 录入今日日志 -> 询问 AI -> 渲染建议卡片 -> 采纳并更新训练计划
```

---

## 三、Sprint 0 - 项目准备

### Task 0.1 初始化 Vite React 项目

- **估时**：30 分钟
- **依赖**：无
- **目标**：创建可运行的 React 前端项目。
- **主要操作**：
  - 使用 Vite 初始化 React 项目。
  - 安装 Tailwind CSS。
  - 配置 `.gitignore`，确保 `.env` 不被提交。
  - 创建基础目录：`src/tabs`、`src/components`、`src/utils`、`src/api`。
- **完成信号**：
  - `npm install` 成功。
  - `npm run dev` 能启动本地页面。
  - 页面显示 FitLoop 初始界面。
- **验证方式**：
  - 浏览器打开本地地址，确认无白屏。
  - 检查 `.env` 已被 `.gitignore` 忽略。

### Task 0.2 创建基础项目文档

- **估时**：30 分钟
- **依赖**：Task 0.1
- **目标**：满足工作区规则中文档同步要求。
- **主要操作**：
  - 创建 `README.md`。
  - 创建 `ARCHITECTURE.md`。
  - 在 `README.md` 中写明安装、运行、API Key 配置和 demo 路径。
  - 在 `ARCHITECTURE.md` 中写明当前目录结构、模块职责和数据流。
- **完成信号**：
  - 两份文档存在且内容与当前项目结构一致。
- **验证方式**：
  - 按 `README.md` 命令重新启动项目。

---

## 四、Sprint 1 - 核心骨架与本地数据

### Task 1.1 搭建 App Tab 框架

- **估时**：45 分钟
- **依赖**：Task 0.1
- **目标**：形成 4 个主页面入口。
- **主要操作**：
  - 在 `src/App.jsx` 中实现顶部导航。
  - 创建 `ProfileTab.jsx`、`PlanTab.jsx`、`TodayTab.jsx`、`CoachTab.jsx` 空页面。
  - 点击导航可切换 Tab。
  - 使用 Tailwind 做基础深色运动风格。
- **完成信号**：
  - 页面有“我的档案、训练计划、今日日志、AI 教练”4 个 Tab。
  - 切换 Tab 不刷新页面。
- **验证方式**：
  - 手动点击 4 个 Tab，确认对应页面标题正常显示。

### Task 1.2 实现 localStorage 工具与默认数据

- **估时**：45 分钟
- **依赖**：Task 1.1
- **目标**：统一管理本地数据读写。
- **主要操作**：
  - 创建 `src/utils/storage.js`。
  - 实现 `loadStorage(key, fallback)` 和 `saveStorage(key, value)`。
  - 创建 `src/utils/defaultData.js`，定义默认 profile、weeklyPlan、dailyLog、chatHistory。
  - 所有默认字段对齐 `docs/plan.md` 和 `docs/fitness_coach_mvp_spec.md`。
- **完成信号**：
  - localStorage 没有数据时，页面能读取默认数据。
  - 写入非法 JSON 时，读取函数能回退到 fallback。
- **验证方式**：
  - 手动清空浏览器 localStorage 后刷新页面，确认页面不崩溃。

### Task 1.3 实现计算工具

- **估时**：45 分钟
- **依赖**：Task 1.2
- **目标**：封装重量、日期、BMR/TDEE 计算。
- **主要操作**：
  - 创建 `src/utils/calc.js`。
  - 实现 `getTodayKey()`、`getTodayStr()`。
  - 实现 `getExerciseKg(exercise, oneRM)`。
  - 实现 `calcBMR(profileBasic)`。
  - 实现 `calcTrainingKcal(exercises, oneRM)`。
  - 实现 `calcTDEE(profile, weeklyPlan, dailyLog)`。
- **完成信号**：
  - 1RM 百分比动作能计算实际重量。
  - 固定 kg 动作能直接返回 kg。
  - 休息日训练消耗为 0。
- **验证方式**：
  - 在页面临时展示或控制台检查：深蹲 120kg 的 75% 显示 90kg。

---

## 五、Sprint 2 - 档案与训练计划

### Task 2.1 实现我的档案 Tab

- **估时**：45 分钟
- **依赖**：Task 1.2
- **目标**：用户能录入基础档案和训练目标。
- **主要操作**：
  - 在 `ProfileTab.jsx` 中实现表单。
  - 字段包括姓名、性别、年龄、身高、当前体重、目标体重、训练目标、备注。
  - 字段包括三大项 1RM：深蹲、卧推、硬拉。
  - 表单修改后保存到 `fitloop_profile`。
- **完成信号**：
  - 刷新页面后，档案数据仍保留。
  - 数字字段按数字保存，不保存为空字符串。
- **验证方式**：
  - 填写“体重 75kg，深蹲 120kg，目标增肌减脂”，刷新后检查数据仍存在。

### Task 2.2 实现训练计划周视图

- **估时**：45 分钟
- **依赖**：Task 1.3、Task 2.1
- **目标**：展示一周训练计划和动作实际重量。
- **主要操作**：
  - 在 `PlanTab.jsx` 中展示 Monday 到 Sunday。
  - 每天显示训练类型和动作数量。
  - 点击某天展开动作列表。
  - 动作显示名称、实际重量、组数、次数、备注。
- **完成信号**：
  - 百分比动作显示类似 `75% -> 90kg`。
  - 固定重量动作显示类似 `80kg`。
- **验证方式**：
  - 修改档案中的深蹲 1RM 后，训练计划中的实际重量随之变化。

### Task 2.3 实现动作编辑组件

- **估时**：60 分钟
- **依赖**：Task 2.2
- **目标**：用户能新增和编辑单个训练动作。
- **主要操作**：
  - 创建 `src/components/ExerciseEditor.jsx`。
  - 支持动作名称、重量来源、参考 1RM、百分比、固定 kg、组数、次数、RPE、备注。
  - 重量来源支持“挂钩 1RM 百分比”和“直接填写 kg”。
  - 编辑器只负责单个动作数据，不直接操作 localStorage。
- **完成信号**：
  - 新增动作时能生成完整 exercise 对象。
  - 百分比模式下 `kg` 为 `null`。
  - 固定 kg 模式下 `ref1RM` 和 `pct` 为 `null`。
- **验证方式**：
  - 新增“深蹲 75% 4 组 6 次”和“罗马尼亚硬拉 80kg 3 组 10 次”，检查展示正确。

### Task 2.4 实现训练计划增删改

- **估时**：60 分钟
- **依赖**：Task 2.3
- **目标**：训练计划可维护并保存。
- **主要操作**：
  - 在 `PlanTab.jsx` 中接入 `ExerciseEditor`。
  - 支持新增动作、编辑动作、删除动作。
  - 支持修改每天训练类型：腿日、推日、拉日、有氧、rest。
  - 每次修改保存到 `fitloop_weeklyPlan`。
- **完成信号**：
  - 训练计划刷新后仍保留。
  - 删除动作不会影响其他日期。
- **验证方式**：
  - 将周一设置为腿日，新增深蹲动作，刷新后检查仍存在。

---

## 六、Sprint 3 - 今日日志与上下文数据

### Task 3.1 实现今日日志表单

- **估时**：45 分钟
- **依赖**：Task 1.2
- **目标**：用户能记录今天的体重、饮食和训练状态。
- **主要操作**：
  - 在 `TodayTab.jsx` 中读取 `getTodayStr()`。
  - 表单字段包括体重、热量、蛋白质、睡眠、疲劳度、是否完成训练、训练备注。
  - 保存到 `fitloop_dailyLog[today]`。
- **完成信号**：
  - 刷新页面后，今天的日志仍保留。
  - 未填写的可选字段不导致页面崩溃。
- **验证方式**：
  - 录入“体重 82kg，热量 2100kcal，疲劳度 3，备注：深蹲第3组没完成”，刷新后检查仍存在。

### Task 3.2 展示今日计划只读摘要

- **估时**：30 分钟
- **依赖**：Task 2.4、Task 3.1
- **目标**：用户录日志时能对照今天训练计划。
- **主要操作**：
  - 在 `TodayTab.jsx` 中读取 `weeklyPlan[todayKey]`。
  - 只读展示今日训练类型和动作摘要。
  - 休息日显示“今天是休息日”。
- **完成信号**：
  - 训练日显示动作列表。
  - 休息日不显示空白异常。
- **验证方式**：
  - 分别把今天设置为训练日和 rest，检查文案变化。

### Task 3.3 实现 System Prompt 构建

- **估时**：60 分钟
- **依赖**：Task 1.3、Task 2.4、Task 3.1
- **目标**：把最新用户状态打包给 AI。
- **主要操作**：
  - 创建 `src/utils/prompt.js`。
  - 实现 `buildSystemPrompt(profile, weeklyPlan, dailyLog)`。
  - 包含基本信息、三大项 1RM、本周训练计划、近 14 天体重、近 7 天饮食、近 7 天训练情况、今日 TDEE。
  - 没有数据时使用“暂无记录”或“未记录”。
- **完成信号**：
  - prompt 文本包含用户最新档案、计划、日志。
  - 新用户空日志不会报错。
- **验证方式**：
  - 在 AI 教练页临时输出上下文预览，检查是否包含“深蹲第3组没完成”和 TDEE 数据。

---

## 七、Sprint 4 - AI 教练闭环

### Task 4.1 实现 DeepSeek API 封装

- **估时**：45 分钟
- **依赖**：Task 3.3
- **目标**：统一调用 AI 接口。
- **主要操作**：
  - 创建 `src/api/deepseek.js`。
  - 从 `import.meta.env.VITE_DEEPSEEK_API_KEY` 读取 API Key。
  - 使用 `POST https://api.deepseek.com/chat/completions`。
  - 返回 `data.choices[0].message.content`。
  - API Key 缺失、网络失败、非 2xx 响应都返回可展示错误。
- **完成信号**：
  - API 调用函数可被 `CoachTab` 调用。
  - API 失败不会导致页面崩溃。
- **验证方式**：
  - 不配置 `.env` 时，AI 教练显示明确错误提示。

### Task 4.2 实现 AI 教练对话 UI

- **估时**：60 分钟
- **依赖**：Task 4.1
- **目标**：用户能在页面内与 AI 对话。
- **主要操作**：
  - 在 `CoachTab.jsx` 中实现聊天气泡区。
  - 实现输入框和发送按钮。
  - 每次发送前调用 `buildSystemPrompt()`。
  - 保存 `fitloop_chatHistory`，只保留最近 20 条。
  - 请求中显示加载状态，避免重复发送。
- **完成信号**：
  - 用户消息和 AI 回复都出现在对话区。
  - 刷新后历史对话仍保留。
- **验证方式**：
  - 输入“最近疲劳度有点高，要不要调整计划”，确认请求被触发并展示结果。

### Task 4.3 实现上下文预览

- **估时**：30 分钟
- **依赖**：Task 4.2
- **目标**：课堂 demo 时能证明 AI 读取了最新数据。
- **主要操作**：
  - 在 `CoachTab.jsx` 增加“当前上下文预览”折叠区域。
  - 展示本次发送前生成的 system prompt。
  - 默认折叠，点击展开。
- **完成信号**：
  - 展开后能看到档案、训练计划、日志和 TDEE。
- **验证方式**：
  - 修改今日日志后回到 AI 教练，预览内容同步变化。

### Task 4.4 实现 AI 返回 JSON 解析

- **估时**：45 分钟
- **依赖**：Task 4.2
- **目标**：识别 AI 是否给出了结构化训练计划建议。
- **主要操作**：
  - 创建 `src/utils/aiResponse.js`。
  - 实现 `parseAiResponse(content)`。
  - 检测 `---JSON---` 分隔符。
  - 分离正文和 JSON。
  - JSON 解析失败时降级为纯文本展示。
- **完成信号**：
  - 含 JSON 的回复能解析出 `text` 和 `suggestion`。
  - 纯文本回复不生成 suggestion。
  - 错误 JSON 不让页面崩溃。
- **验证方式**：
  - 使用 mock 文本分别测试：纯文本、合法 JSON、非法 JSON。

### Task 4.5 实现采纳建议卡片

- **估时**：60 分钟
- **依赖**：Task 4.4、Task 2.4
- **目标**：用户能看到 AI 建议变更并选择采纳。
- **主要操作**：
  - 创建 `src/components/AdoptCard.jsx`。
  - 显示建议日期、summary 和 changes 对比。
  - 提供“采纳并更新计划”和“忽略”按钮。
  - 组件只负责展示和触发回调，不直接读取 API。
- **完成信号**：
  - AI 回复含合法 JSON 时出现采纳卡片。
  - 点击忽略后卡片消失或标记已忽略。
- **验证方式**：
  - 用 mock AI 回复生成“深蹲 75% -> 70%”卡片，检查展示正确。

### Task 4.6 实现一键采纳写回训练计划

- **估时**：45 分钟
- **依赖**：Task 4.5
- **目标**：完成 FitLoop MVP 的核心闭环。
- **主要操作**：
  - 在 `CoachTab.jsx` 或独立工具函数中实现 `adoptPlanChange(day, changes)`。
  - 从 `fitloop_weeklyPlan` 找到目标日期和动作。
  - 按 `field` 更新 `newValue`。
  - 保存回 localStorage。
  - 写回成功后给用户明确反馈。
- **完成信号**：
  - 点击采纳后，训练计划页中对应动作字段变化。
  - 找不到动作时显示失败提示，不静默失败。
- **验证方式**：
  - 完整跑通：AI mock 返回深蹲 pct 从 0.75 改为 0.70，采纳后训练计划显示新重量。

---

## 八、Sprint 5 - 最小验证与课程交付

### Task 5.1 建立最小验证清单

- **估时**：45 分钟
- **依赖**：Task 4.6
- **目标**：满足课程对 TDD 或最小验证方案的要求。
- **主要操作**：
  - 在 `docs/verification.md` 中记录核心成功路径。
  - 记录 2-3 个边界情况。
  - 记录 1 个失败场景。
  - 每项写明前置数据、操作步骤、预期结果、实际结果。
- **完成信号**：
  - 文档能直接放进课程报告。
- **验证方式**：
  - 按文档从头操作一遍，确认步骤可复现。

### Task 5.2 补齐 README 和 ARCHITECTURE

- **估时**：45 分钟
- **依赖**：Task 4.6
- **目标**：保证助教可以运行和理解项目。
- **主要操作**：
  - 更新 `README.md`：安装、运行、API Key、测试、demo 步骤。
  - 更新 `ARCHITECTURE.md`：目录结构、模块职责、数据流、AI 调用链路。
  - 明确 localStorage 数据不会云端同步。
- **完成信号**：
  - 新同学按 README 能启动项目。
  - ARCHITECTURE 能说明核心闭环如何流动。
- **验证方式**：
  - 在干净环境中按 README 命令运行。

### Task 5.3 整理课堂 demo 脚本

- **估时**：30 分钟
- **依赖**：Task 5.1
- **目标**：保证 7 分钟展示有清晰路径。
- **主要操作**：
  - 在 `docs/demo_script.md` 中写 1 分钟项目介绍。
  - 写 3-4 分钟操作流程。
  - 写 1 分钟验证结果。
  - 写 1 分钟局限与后续迭代。
- **完成信号**：
  - demo 脚本覆盖“问题、方案、核心功能、验证结果、下一步迭代”。
- **验证方式**：
  - 按脚本计时演练一次，总时长不超过 7 分钟。

---

## 九、Sprint 6 - 加分项（核心闭环稳定后再做）

### Task 6.1 体重趋势折线图

- **估时**：45 分钟
- **依赖**：Task 3.1
- **目标**：展示近 14 天体重变化。
- **主要操作**：
  - 安装并使用 Recharts。
  - 创建 `src/components/WeightChart.jsx`。
  - 在 `TodayTab.jsx` 中展示折线图。
- **完成信号**：
  - 有 2 条以上体重记录时显示折线。
  - 记录不足时显示友好空状态。
- **验证方式**：
  - 手动造 3 天体重数据，检查图表渲染。

### Task 6.2 数据导入导出

- **估时**：45 分钟
- **依赖**：Task 4.6
- **目标**：降低 localStorage 丢失风险。
- **主要操作**：
  - 导出 profile、weeklyPlan、dailyLog、chatHistory 为 JSON 文件。
  - 支持导入 JSON 并覆盖当前本地数据。
  - 导入前做基础字段检查。
- **完成信号**：
  - 能导出备份文件。
  - 清空 localStorage 后可通过导入恢复。
- **验证方式**：
  - 导出、清空、导入、刷新，确认数据恢复。

### Task 6.3 AI 流式输出

- **估时**：60 分钟
- **依赖**：Task 4.2
- **目标**：提升演示观感。
- **主要操作**：
  - DeepSeek 请求增加 `stream: true`。
  - 使用 SSE 逐段渲染回复。
  - 保留非流式调用作为降级方案。
- **完成信号**：
  - AI 回复能逐字或分段出现。
  - 流式失败时仍能显示错误提示。
- **验证方式**：
  - 发送一次长问题，观察回复逐步渲染。

---

## 十、最终验收任务

### Task 7.1 核心成功路径验收

- **估时**：30 分钟
- **依赖**：Task 4.6
- **操作路径**：
  1. 填写档案：体重 75kg，深蹲 1RM 120kg，目标增肌减脂。
  2. 设置周一训练计划：深蹲 75% x 4 x 6。
  3. 录入今日日志：体重 82kg，热量 2100kcal，疲劳度 3，备注“深蹲第3组没完成”。
  4. 打开 AI 教练，发送“最近疲劳度有点高，要不要调整计划”。
  5. 看到 AI 回复和采纳卡片。
  6. 点击采纳。
  7. 回到训练计划页，确认深蹲计划已更新。
- **完成信号**：
  - 核心闭环完整跑通。

### Task 7.2 边界与失败场景验收

- **估时**：45 分钟
- **依赖**：Task 7.1
- **验证项**：
  - 今天为 rest 时，prompt 中训练消耗为 0。
  - 动作使用固定 kg 时，重量显示和 TDEE 计算正常。
  - 新用户没有日志时，AI 教练不崩溃。
  - Profile 未填写时，不发送 AI 请求。
  - API Key 缺失或错误时，页面显示错误提示。
  - AI 返回纯文本时，不渲染采纳卡片。
- **完成信号**：
  - 每项都有实际结果记录，可放入 `docs/verification.md`。

---

## 十一、任务完成顺序摘要

| 顺序 | 任务 | 预计时间 |
| --- | --- | --- |
| 1 | Task 0.1 初始化 Vite React 项目 | 30min |
| 2 | Task 0.2 创建基础项目文档 | 30min |
| 3 | Task 1.1 搭建 App Tab 框架 | 45min |
| 4 | Task 1.2 实现 localStorage 工具与默认数据 | 45min |
| 5 | Task 1.3 实现计算工具 | 45min |
| 6 | Task 2.1 实现我的档案 Tab | 45min |
| 7 | Task 2.2 实现训练计划周视图 | 45min |
| 8 | Task 2.3 实现动作编辑组件 | 60min |
| 9 | Task 2.4 实现训练计划增删改 | 60min |
| 10 | Task 3.1 实现今日日志表单 | 45min |
| 11 | Task 3.2 展示今日计划只读摘要 | 30min |
| 12 | Task 3.3 实现 System Prompt 构建 | 60min |
| 13 | Task 4.1 实现 DeepSeek API 封装 | 45min |
| 14 | Task 4.2 实现 AI 教练对话 UI | 60min |
| 15 | Task 4.3 实现上下文预览 | 30min |
| 16 | Task 4.4 实现 AI 返回 JSON 解析 | 45min |
| 17 | Task 4.5 实现采纳建议卡片 | 60min |
| 18 | Task 4.6 实现一键采纳写回训练计划 | 45min |
| 19 | Task 5.1 建立最小验证清单 | 45min |
| 20 | Task 5.2 补齐 README 和 ARCHITECTURE | 45min |
| 21 | Task 5.3 整理课堂 demo 脚本 | 30min |

---

*文档版本：v1.0 | 对应 idea.md v1.0 / plan.md v1.0 / fitness_coach_mvp_spec.md v1.2*
