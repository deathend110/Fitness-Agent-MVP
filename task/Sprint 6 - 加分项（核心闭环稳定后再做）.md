### Task 6.1 体重趋势折线图

- **估时**：45 分钟
- **依赖**：Task 3.1
- **目标**：展示近 14 天体重变化。
- **主要操作**：
  - 安装并使用 Recharts。
  - 创建 `src/components/WeightChart.jsx`。
  - 在 `TodayTab.jsx` 中展示折线图。
- **完成信号**：
  - 有 2 条以上体重记录时显示折线。
  - 记录不足时显示友好空状态。
- **验证方式**：
  - 手动造 3 天体重数据，检查图表渲染。

### Task 6.2 数据导入导出

- **估时**：45 分钟
- **依赖**：Task 4.6
- **目标**：降低 localStorage 丢失风险。
- **主要操作**：
  - 导出 profile、weeklyPlan、dailyLog、chatHistory 为 JSON 文件。
  - 支持导入 JSON 并覆盖当前本地数据。
  - 导入前做基础字段检查。
- **完成信号**：
  - 能导出备份文件。
  - 清空 localStorage 后可通过导入恢复。
- **验证方式**：
  - 导出、清空、导入、刷新，确认数据恢复。

### Task 6.3 AI 流式输出

- **估时**：60 分钟
- **依赖**：Task 4.2
- **目标**：提升演示观感。
- **主要操作**：
  - DeepSeek 请求增加 `stream: true`。
  - 使用 SSE 逐段渲染回复。
  - 保留非流式调用作为降级方案。
- **完成信号**：
  - AI 回复能逐字或分段出现。
  - 流式失败时仍能显示错误提示。
- **验证方式**：
  - 发送一次长问题，观察回复逐步渲染。