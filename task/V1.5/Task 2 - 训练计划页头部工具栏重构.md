# Task 2 训练计划页头部工具栏重构

- **估时**：35 分钟
- **依赖**：Task 1
- **目标**：把训练计划页顶部升级为效果稿中的“标题 + 周区间 + 周编号 + 图例 + 计划设置占位入口”结构。
- **主要文件**：
  - `src/tabs/PlanTab.jsx`
  - `src/components/` 下训练计划页头部相关组件
  - `src/utils/` 下训练计划头部展示模型相关文件
- **主要操作**：
  - 为训练计划页增加新的顶部工具栏区域。
  - 展示本周标题、周区间、周编号徽标。
  - 增加主项 / 辅项图例展示。
  - 保留“计划设置”按钮作为未来复杂周期计划与经典计划库页面的调出入口，但当前阶段不实现内部功能。
  - 删除“AI 优化建议”按钮，不在训练计划页重复提供该入口。
  - 删除“列表视图”切换目标，不为当前阶段保留无用入口。
  - 让头部工具栏宽度、对齐和压缩策略适配 16:9 全屏布局，不把横向滚动压力转嫁给内容区。
- **完成信号**：
  - 训练计划页头部结构接近效果稿。
  - 工具栏与下方内容区层级清楚，不拥挤。
  - “计划设置”按钮存在但不误导为已实现功能。
  - 不再出现“AI 优化建议”与“列表视图”入口。
- **验证方式**：
  - 打开训练计划页，确认头部信息完整存在。
  - 检查“计划设置”按钮、图例、周编号与日期区间在桌面宽度下不挤压。
  - 在全屏桌面宽度下确认头部区域不会把页面撑出横向滚动。
