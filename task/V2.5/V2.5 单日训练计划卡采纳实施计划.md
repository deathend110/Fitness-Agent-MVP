# V2.5 单日训练计划卡采纳实施计划

> **For agentic workers:** REQUIRED SUB-SKILL：使用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans` 按任务执行。步骤用 checkbox 语法追踪。
> 上游设计：[V2.5 单日训练计划卡采纳设计.md](<./V2.5 单日训练计划卡采纳设计.md>)
> 参考实现：[V2.4 AI 教练消息附件回显实施计划.md](<../V2.4/V2.4 AI 教练消息附件回显实施计划.md>)

**Goal:** 让 AI 教练生成的“单日训练计划卡”在用户确认后，能够把目标日期的训练类型和完整动作列表直接写回训练计划页，同时保留旧版字段 patch proposal 的兼容能力。

**Architecture:** 在现有 `proposal -> 用户确认 -> commit` 框架上新增 `day_plan_replace` proposal 类型。后端按 `proposal.kind` 分流处理旧版 `field_changes` 与新版整日计划替换；前端继续复用现有采纳入口，但扩展卡片渲染、commit 后周计划合并和浏览器级回归验证。

**Tech Stack:** FastAPI / SQLAlchemy async / pytest / httpx / React / Vite / Node test / Playwright。

---

## 文件结构与职责

- Modify: `backend/agent/adopt_plan.py`
  - 升级 proposal store 结构，新增整日计划 proposal 构建、校验、归一化与 commit 能力。
- Modify: `backend/agent/tool_calling.py`
  - 新增 `propose_day_plan_replace` tool schema 与 handler。
- Modify: `backend/api/tools.py`
  - 扩展 `/api/tools/plan/propose` 请求结构，支持 `kind=day_plan_replace`。
- Modify: `backend/schemas/__init__.py`
  - 扩展 proposal 请求/响应相关 schema，兼容 `dayPlan`。
- Modify: `backend/tests/test_plan_tools.py`
  - 为整日计划 proposal 与 commit 增加接口级测试。
- Modify: `backend/tests/test_background_worker.py`
  - 验证后台思考链路生成 `day_plan_replace` proposal 后仍可 commit。
- Modify: `backend/tests/test_chat_stream.py`
  - 验证 Agent tool loop 事件可输出新版 proposal 结构。
- Modify: `src/utils/adoptCard.js`
  - 支持把 `day_plan_replace` proposal 转成“单日训练计划预览卡”展示模型。
- Modify: `src/components/AdoptCard.jsx`
  - 渲染两类卡片：旧版字段变更卡、单日训练计划卡。
- Modify: `src/tabs/CoachTab.jsx`
  - commit 成功后使用安全 merge 策略更新 `weeklyPlan`，避免丢失 `weekMeta` 等顶层元数据。
- Create: `src/utils/weeklyPlanCommit.js`
  - 封装 commit 返回计划与当前本地计划的合并逻辑。
- Modify: `tests/adoptCard.test.js`
  - 增加 `day_plan_replace` 卡片模型测试。
- Create: `tests/weeklyPlanCommit.test.js`
  - 验证 commit 后计划合并不丢顶层元数据。
- Modify: `tests/coachComposer.test.js`
  - 增加新版 proposal 采纳链路的源码契约。
- Modify: `tests/e2e/coach_browser_smoke.py`
  - 扩展浏览器自动化，验证整日计划卡采纳后训练计划页同步刷新。
- Modify: `README.md`
  - 补一条 V2.5 的 README 使用说明，不写阶段性日志，只更新可用能力描述。
- Modify: `ARCHITECTURE.md`
  - 补充 `day_plan_replace` proposal 与 commit 分流架构说明。

---

## Task 33: 后端 proposal store 升级为支持 `day_plan_replace`

**Files:**
- Modify: `backend/agent/adopt_plan.py`
- Test: `backend/tests/test_plan_tools.py`

- [ ] **Step 1: 先写后端失败测试**

在 `backend/tests/test_plan_tools.py` 增加两个新用例。

第一个：验证可以构造整日计划 proposal。

```python
def build_day_plan_payload() -> dict:
    return {
        "type": "腿日",
        "exercises": [
            {
                "name": "深蹲",
                "tier": "main",
                "template": {
                    "loadMode": "percentage",
                    "ref1RM": "squat",
                    "setType": "straight",
                    "sets": 3,
                    "repsText": "5",
                },
                "instance": {
                    "pct": 0.7,
                    "kg": None,
                    "rpe": 7,
                    "note": "恢复周主项",
                },
                "ref1RM": "squat",
                "pct": 0.7,
                "kg": None,
                "sets": 3,
                "reps": 5,
                "rpe": 7,
                "note": "恢复周主项",
            }
        ],
    }


def test_build_day_plan_replace_proposal_preserves_day_plan_payload() -> None:
    plan = build_weekly_plan()
    proposal = build_day_plan_replace_proposal(
        current_plan=plan,
        session_id=1,
        day="Monday",
        summary="改成恢复型腿日",
        day_plan=build_day_plan_payload(),
    )

    assert proposal.kind == "day_plan_replace"
    assert proposal.card["dayPlan"]["exercises"][0]["name"] == "深蹲"
    assert proposal.validation.ok is True
```

第二个：验证 commit 后整日计划替换目标日。

```python
def test_commit_day_plan_replace_overwrites_target_day() -> None:
    plan = build_weekly_plan()
    proposal = build_day_plan_replace_proposal(
        current_plan=plan,
        session_id=1,
        day="Monday",
        summary="改成恢复型腿日",
        day_plan=build_day_plan_payload(),
    )

    committed = commit_plan_proposal(
        current_plan=plan,
        proposal_id=proposal.proposal_id,
        confirmed_by_user=True,
    )

    assert committed.ok is True
    assert committed.next_plan["Monday"]["type"] == "腿日"
    assert committed.next_plan["Monday"]["exercises"][0]["sets"] == 3
    assert committed.next_plan["Monday"]["exercises"][0]["rpe"] == 7
```

- [ ] **Step 2: 跑测试确认当前失败**

Run:

```powershell
uv run pytest backend\tests\test_plan_tools.py -q
```

Expected：

- 失败原因是 `build_day_plan_replace_proposal` / `commit_plan_proposal` 当前不存在，proposal 结构也没有 `kind`。

- [ ] **Step 3: 升级 proposal 数据模型**

在 `backend/agent/adopt_plan.py` 中新增统一 proposal dataclass，并保留旧字段兼容：

```python
@dataclass(frozen=True)
class StoredPlanProposal:
    proposal_id: str
    session_id: int | None
    kind: str
    day: str
    summary: str
    payload: dict[str, Any]
    card: dict[str, Any]
    validation: AdoptPlanResult
    status: str = "pending"
```

并把 `_proposal_store` 从：

```python
_proposal_store: dict[str, PlanChangeProposal] = {}
```

改成：

```python
_proposal_store: dict[str, StoredPlanProposal] = {}
```

- [ ] **Step 4: 增加整日计划 proposal 构建与 commit 分流**

在 `backend/agent/adopt_plan.py` 中新增：

```python
def build_day_plan_replace_proposal(
    *,
    current_plan: dict[str, Any],
    session_id: int | None,
    day: str,
    summary: str,
    day_plan: dict[str, Any],
) -> StoredPlanProposal:
    normalized_day_plan = normalize_day_plan_payload(day_plan)
    validation = validate_day_plan_replace(current_plan, day, normalized_day_plan)
    proposal_id = uuid4().hex
    proposal = StoredPlanProposal(
        proposal_id=proposal_id,
        session_id=session_id,
        kind="day_plan_replace",
        day=day,
        summary=summary,
        payload={"dayPlan": normalized_day_plan},
        card={
            "proposalId": proposal_id,
            "kind": "day_plan_replace",
            "day": day,
            "summary": summary,
            "dayPlan": normalized_day_plan,
            "status": "pending" if validation.ok else "invalid",
        },
        validation=validation,
        status="pending" if validation.ok else "invalid",
    )
    _proposal_store[proposal_id] = proposal
    return proposal
```

并新增统一 commit 入口：

```python
def commit_plan_proposal(
    current_plan: dict[str, Any],
    proposal_id: str,
    *,
    confirmed_by_user: bool,
) -> AdoptPlanResult:
    proposal = _proposal_store.get(proposal_id)
    if proposal is None:
        return _build_failure_result(current_plan, "未找到计划修改提议，无法写回。")
    if proposal.status != "pending":
        return _build_failure_result(current_plan, "该计划修改提议已处理，不能重复提交。")
    if not confirmed_by_user:
        return _build_failure_result(current_plan, "计划写回必须来自用户确认。")

    if proposal.kind == "field_changes":
        result = adopt_plan_change(current_plan, proposal.day, proposal.payload["changes"])
    elif proposal.kind == "day_plan_replace":
        result = adopt_day_plan_replace(current_plan, proposal.day, proposal.payload["dayPlan"])
    else:
        return _build_failure_result(current_plan, "未识别的计划提议类型，无法写回。")

    next_status = "committed" if result.ok else "failed"
    _proposal_store[proposal_id] = StoredPlanProposal(
        **{**proposal.__dict__, "status": next_status, "card": {**proposal.card, "status": next_status}}
    )
    return result
```

- [ ] **Step 5: 运行后端验证**

Run:

```powershell
uv run pytest backend\tests\test_plan_tools.py -q
```

Expected：

- 新增 proposal store 测试通过
- 旧 `field_changes` proposal 测试仍通过

- [ ] **Step 6: 提交本子任务**

```powershell
git add backend/agent/adopt_plan.py backend/tests/test_plan_tools.py
git commit -m "升级计划提议存储以支持单日计划卡"
```

## Task 34: 后端增加整日计划校验、归一化与 API 接口支持

**Files:**
- Modify: `backend/agent/adopt_plan.py`
- Modify: `backend/agent/tool_calling.py`
- Modify: `backend/api/tools.py`
- Modify: `backend/schemas/__init__.py`
- Test: `backend/tests/test_plan_tools.py`
- Test: `backend/tests/test_chat_stream.py`
- Test: `backend/tests/test_background_worker.py`

- [ ] **Step 1: 先写接口失败测试**

在 `backend/tests/test_plan_tools.py` 增加接口测试：

```python
@pytest.mark.asyncio
async def test_plan_propose_endpoint_accepts_day_plan_replace(api_client: AsyncClient) -> None:
    original_plan = build_weekly_plan()
    await api_client.put("/api/weekly-plan", json=original_plan)

    response = await api_client.post(
        "/api/tools/plan/propose",
        json={
            "kind": "day_plan_replace",
            "sessionId": 3,
            "day": "Monday",
            "summary": "改成恢复型腿日",
            "dayPlan": build_day_plan_payload(),
        },
    )

    body = response.json()
    assert response.status_code == 200
    assert body["validation"]["ok"] is True
    assert body["card"]["kind"] == "day_plan_replace"
    assert body["card"]["dayPlan"]["exercises"][0]["name"] == "深蹲"
```

以及 commit 接口测试：

```python
@pytest.mark.asyncio
async def test_plan_commit_endpoint_writes_day_plan_replace(api_client: AsyncClient) -> None:
    original_plan = build_weekly_plan()
    await api_client.put("/api/weekly-plan", json=original_plan)

    proposal = await api_client.post(
        "/api/tools/plan/propose",
        json={
            "kind": "day_plan_replace",
            "sessionId": 5,
            "day": "Monday",
            "summary": "改成恢复型腿日",
            "dayPlan": build_day_plan_payload(),
        },
    )
    proposal_id = proposal.json()["proposalId"]

    committed = await api_client.post("/api/tools/plan/commit", json={"proposalId": proposal_id})
    assert committed.status_code == 200
    assert committed.json()["ok"] is True
    assert committed.json()["plan"]["Monday"]["exercises"][0]["name"] == "深蹲"
```

在 `backend/tests/test_chat_stream.py` 增加 tool loop 事件测试，断言 `proposal.kind == "day_plan_replace"`。

在 `backend/tests/test_background_worker.py` 增加后台任务 proposal 测试，断言后台返回的 `suggestion.kind == "day_plan_replace"` 且 commit 成功。

- [ ] **Step 2: 跑测试确认当前失败**

Run:

```powershell
uv run pytest backend\tests\test_plan_tools.py backend\tests\test_chat_stream.py backend\tests\test_background_worker.py -q
```

Expected：

- 失败原因是 API schema 不接受 `kind/dayPlan`，tool 调用层也还没有 `propose_day_plan_replace`。

- [ ] **Step 3: 扩展 schema 与工具接口**

在 `backend/schemas/__init__.py` 中新增整日计划 adopt schema：

```python
class PlanProposalDayPlanSchema(BaseModel):
    model_config = ConfigDict(populate_by_name=True)

    type: str
    exercises: list[dict[str, Any]]
```

并扩展 `backend/api/tools.py` 请求模型：

```python
class PlanProposeRequestSchema(BaseModel):
    kind: str = "field_changes"
    sessionId: int | None = None
    day: str
    summary: str = ""
    changes: list[dict[str, Any]] = Field(default_factory=list)
    dayPlan: dict[str, Any] | None = None
```

在 `propose_plan_change()` 中分流：

```python
if payload.kind == "day_plan_replace":
    proposal = build_day_plan_replace_proposal(
        current_plan=current_plan,
        session_id=payload.sessionId,
        day=payload.day,
        summary=payload.summary,
        day_plan=payload.dayPlan or {},
    )
else:
    proposal = build_plan_change_proposal(
        current_plan=current_plan,
        session_id=payload.sessionId,
        day=payload.day,
        changes=payload.changes,
        summary=payload.summary,
    )
```

`commit_plan_change()` 中把：

```python
result = commit_validated_plan_change(...)
```

改为：

```python
result = commit_plan_proposal(
    current_plan,
    payload.proposalId,
    confirmed_by_user=True,
)
```

- [ ] **Step 4: 新增整日计划 tool**

在 `backend/agent/tool_calling.py` 中新增参数模型：

```python
class ProposeDayPlanReplaceToolArgs(BaseModel):
    model_config = ConfigDict(extra="forbid")

    day: str
    summary: str = ""
    dayPlan: dict[str, Any]
```

新增 tool 注册：

```python
registry.register(
    RegisteredTool(
        "propose_day_plan_replace",
        "生成单日训练计划替换建议卡；只生成 proposal，不直接写入计划。",
        ProposeDayPlanReplaceToolArgs,
        _propose_day_plan_replace,
    )
)
```

新增 handler：

```python
async def _propose_day_plan_replace(
    session: AsyncSession,
    args: ProposeDayPlanReplaceToolArgs,
) -> dict[str, Any]:
    current_plan = await _get_weekly_plan(session, EmptyToolArgs())
    proposal = build_day_plan_replace_proposal(
        current_plan=current_plan,
        session_id=None,
        day=args.day,
        summary=args.summary,
        day_plan=args.dayPlan,
    )
    return {
        "proposal": proposal.card,
        "validation": {
            "ok": proposal.validation.ok,
            "message": proposal.validation.message,
        },
    }
```

- [ ] **Step 5: 增加整日计划归一化与写回**

在 `backend/agent/adopt_plan.py` 中新增：

```python
def normalize_day_plan_payload(day_plan: dict[str, Any]) -> dict[str, Any]:
    safe_day_plan = day_plan if _is_plain_object(day_plan) else {}
    safe_exercises = safe_day_plan.get("exercises") if isinstance(safe_day_plan.get("exercises"), list) else []
    normalized_exercises = [
        _normalize_planned_exercise(exercise, exercise.get("id") or f"proposal-{index}")
        for index, exercise in enumerate(safe_exercises)
        if _is_plain_object(exercise)
    ]
    return {
        "type": _read_string_value(safe_day_plan.get("type")) or "rest",
        "exercises": normalized_exercises,
    }
```

和：

```python
def adopt_day_plan_replace(
    weekly_plan: dict[str, Any] | None,
    day: str | None,
    day_plan: dict[str, Any] | None,
) -> AdoptPlanResult:
    safe_plan = deepcopy(weekly_plan) if _is_plain_object(weekly_plan) else {}
    day_key = day.strip() if isinstance(day, str) else ""

    if not day_key or day_key not in safe_plan or day_key not in WEEKDAY_ORDER:
        return _build_failure_result(safe_plan, f"未找到 {day_key or '目标日期'} 的训练计划，无法采纳该建议。")

    normalized_day_plan = normalize_day_plan_payload(day_plan or {})
    if not isinstance(normalized_day_plan.get("exercises"), list):
        return _build_failure_result(safe_plan, "单日训练计划格式不合法，无法采纳该建议。")

    return AdoptPlanResult(
        ok=True,
        message=SUCCESS_MESSAGE,
        next_plan={
            **safe_plan,
            day_key: normalized_day_plan,
        },
    )
```

- [ ] **Step 6: 运行后端验证**

Run:

```powershell
uv run pytest backend\tests\test_plan_tools.py backend\tests\test_chat_stream.py backend\tests\test_background_worker.py -q
```

Expected：

- `day_plan_replace` proposal 生成与 commit 测试通过
- 旧 `field_changes` proposal 测试仍通过
- 流式与后台任务链路可返回新版 proposal

- [ ] **Step 7: 提交本子任务**

```powershell
git add backend/agent/adopt_plan.py backend/agent/tool_calling.py backend/api/tools.py backend/schemas/__init__.py backend/tests/test_plan_tools.py backend/tests/test_chat_stream.py backend/tests/test_background_worker.py
git commit -m "新增单日训练计划卡提议与写回接口"
```

## Task 35: 前端渲染单日训练计划卡并安全合并 commit 结果

**Files:**
- Modify: `src/utils/adoptCard.js`
- Modify: `src/components/AdoptCard.jsx`
- Modify: `src/tabs/CoachTab.jsx`
- Create: `src/utils/weeklyPlanCommit.js`
- Test: `tests/adoptCard.test.js`
- Test: `tests/weeklyPlanCommit.test.js`
- Test: `tests/coachComposer.test.js`

- [ ] **Step 1: 先写前端失败测试**

在 `tests/adoptCard.test.js` 增加：

```javascript
test('buildAdoptCardModel 会把 day_plan_replace proposal 转成单日计划卡模型', () => {
  const card = buildAdoptCardModel({
    proposalId: 'proposal-day-plan',
    kind: 'day_plan_replace',
    day: 'Monday',
    summary: '恢复型腿日',
    dayPlan: {
      type: '腿日',
      exercises: [
        {
          name: '深蹲',
          tier: 'main',
          sets: 3,
          reps: 5,
          pct: 0.7,
          rpe: 7,
          note: '恢复周主项',
        },
      ],
    },
  })

  assert.equal(card.variant, 'dayPlan')
  assert.equal(card.dayLabel, '周一')
  assert.equal(card.dayTypeLabel, '腿日')
  assert.equal(card.exercises[0].name, '深蹲')
})
```

在 `tests/weeklyPlanCommit.test.js` 新增：

```javascript
import { mergeCommittedWeeklyPlan } from '../src/utils/weeklyPlanCommit.js'

test('mergeCommittedWeeklyPlan 会保留本地顶层 weekMeta 并覆盖后端返回的七天计划', () => {
  const currentPlan = {
    weekMeta: { weekNumber: 8 },
    Monday: { type: 'rest', exercises: [] },
    Tuesday: { type: 'rest', exercises: [] },
    Wednesday: { type: 'rest', exercises: [] },
    Thursday: { type: 'rest', exercises: [] },
    Friday: { type: 'rest', exercises: [] },
    Saturday: { type: 'rest', exercises: [] },
    Sunday: { type: 'rest', exercises: [] },
  }

  const committedPlan = {
    Monday: { type: '腿日', exercises: [{ name: '深蹲', sets: 3 }] },
    Tuesday: { type: 'rest', exercises: [] },
    Wednesday: { type: 'rest', exercises: [] },
    Thursday: { type: 'rest', exercises: [] },
    Friday: { type: 'rest', exercises: [] },
    Saturday: { type: 'rest', exercises: [] },
    Sunday: { type: 'rest', exercises: [] },
  }

  const merged = mergeCommittedWeeklyPlan(currentPlan, committedPlan)
  assert.equal(merged.weekMeta.weekNumber, 8)
  assert.equal(merged.Monday.exercises[0].name, '深蹲')
})
```

在 `tests/coachComposer.test.js` 增加源码契约：

```javascript
assert.match(source, /mergeCommittedWeeklyPlan/)
assert.match(source, /onWeeklyPlanChange\(\(currentPlan\) => mergeCommittedWeeklyPlan/)
```

- [ ] **Step 2: 跑测试确认当前失败**

Run:

```powershell
node --test tests\adoptCard.test.js tests\weeklyPlanCommit.test.js tests\coachComposer.test.js
```

Expected：

- 失败原因是 `day_plan_replace` 卡片模型与 `mergeCommittedWeeklyPlan` 还未实现。

- [ ] **Step 3: 实现 commit 结果合并工具**

新建 `src/utils/weeklyPlanCommit.js`：

```javascript
import { getWeekdayOrder, normalizeWeeklyPlan } from './weeklyPlan.js'

export function mergeCommittedWeeklyPlan(currentPlan = {}, committedPlan = {}) {
  const mergedDays = normalizeWeeklyPlan(committedPlan)
  const nextPlan = {
    ...(currentPlan && typeof currentPlan === 'object' ? currentPlan : {}),
  }

  getWeekdayOrder().forEach((dayKey) => {
    nextPlan[dayKey] = mergedDays[dayKey]
  })

  return nextPlan
}
```

- [ ] **Step 4: 扩展卡片展示模型**

在 `src/utils/adoptCard.js` 中增加 `day_plan_replace` 分支：

```javascript
if (suggestion.kind === 'day_plan_replace' && suggestion.dayPlan) {
  const exercises = Array.isArray(suggestion.dayPlan.exercises) ? suggestion.dayPlan.exercises : []
  return {
    variant: 'dayPlan',
    dayLabel: formatDayLabel(suggestion.day),
    dayTypeLabel: suggestion.dayPlan.type || '训练日',
    summary: summary || 'AI 给出了一张单日训练计划调整卡。',
    exercises: exercises.map((exercise, index) => ({
      id: exercise.id || `${index}-${exercise.name || 'exercise'}`,
      name: exercise.name || '未命名动作',
      tier: exercise.tier || 'accessory',
      setsLabel: formatChangeValue('sets', exercise.sets),
      repsLabel: formatChangeValue('reps', exercise.reps),
      loadLabel:
        typeof exercise.pct === 'number'
          ? formatChangeValue('pct', exercise.pct)
          : formatChangeValue('kg', exercise.kg),
      rpeLabel: formatChangeValue('rpe', exercise.rpe),
      note: exercise.note || '',
    })),
  }
}
```

- [ ] **Step 5: 在 AdoptCard 中渲染 dayPlan 形态**

在 `src/components/AdoptCard.jsx` 中增加：

```jsx
if (card.variant === 'dayPlan') {
  return (
    <section className="rounded-2xl border border-fitloop-line bg-white/95 p-4 shadow-sm">
      <p className="text-xs font-semibold uppercase tracking-[0.2em] text-fitloop-orange">
        {card.dayLabel} · {card.dayTypeLabel}
      </p>
      <h3 className="mt-2 text-base font-semibold text-slate-800">单日训练计划调整建议</h3>
      <p className="mt-2 text-sm leading-6 text-slate-600">{card.summary}</p>
      <div className="mt-3 space-y-2">
        {card.exercises.map((exercise) => (
          <div className="rounded-xl border border-fitloop-line/70 bg-slate-50 px-3 py-2" key={exercise.id}>
            <div className="flex items-center justify-between gap-3">
              <span className="text-sm font-semibold text-slate-800">{exercise.name}</span>
              <span className="text-[11px] font-medium text-slate-500">{exercise.tier === 'main' ? '主项' : '辅项'}</span>
            </div>
            <div className="mt-1 flex flex-wrap gap-2 text-xs text-slate-500">
              <span>{exercise.setsLabel}</span>
              <span>{exercise.repsLabel}</span>
              <span>{exercise.loadLabel}</span>
              <span>{exercise.rpeLabel}</span>
            </div>
            {exercise.note ? <p className="mt-1 text-xs text-slate-500">{exercise.note}</p> : null}
          </div>
        ))}
      </div>
      {/* 保持现有采纳/忽略按钮区不变 */}
    </section>
  )
}
```

- [ ] **Step 6: 在 CoachTab 中接入安全合并**

修改 `src/tabs/CoachTab.jsx`：

```javascript
import { mergeCommittedWeeklyPlan } from '../utils/weeklyPlanCommit.js'
```

把：

```javascript
onWeeklyPlanChange(adoptResult.plan)
```

改成：

```javascript
onWeeklyPlanChange((currentPlan) => mergeCommittedWeeklyPlan(currentPlan, adoptResult.plan))
```

这样前端顶层 `weekMeta` 不会被后端返回的 canonical 7 天结构冲掉。

- [ ] **Step 7: 运行前端验证**

Run:

```powershell
node --test tests\adoptCard.test.js tests\weeklyPlanCommit.test.js tests\coachComposer.test.js
```

Expected：

- 单日训练计划卡模型通过
- commit 合并逻辑通过
- 采纳链路源码契约通过

- [ ] **Step 8: 提交本子任务**

```powershell
git add src/utils/adoptCard.js src/components/AdoptCard.jsx src/tabs/CoachTab.jsx src/utils/weeklyPlanCommit.js tests/adoptCard.test.js tests/weeklyPlanCommit.test.js tests/coachComposer.test.js
git commit -m "接入单日训练计划卡前端采纳链路"
```

## Task 36: 浏览器回归验证与文档同步

**Files:**
- Modify: `tests/e2e/coach_browser_smoke.py`
- Modify: `README.md`
- Modify: `ARCHITECTURE.md`

- [ ] **Step 1: 先扩展浏览器自动化用例**

在 `tests/e2e/coach_browser_smoke.py` 中把 suggestion mock 从旧 `changes[]` 升级为新版 proposal：

```python
"suggestion": {
    "proposalId": "proposal-e2e",
    "kind": "day_plan_replace",
    "day": "Monday",
    "summary": "把周一改成恢复型腿日。",
    "dayPlan": {
        "type": "腿日",
        "exercises": [
            {
                "id": "monday-squat-recovery",
                "name": "深蹲",
                "tier": "main",
                "template": {
                    "loadMode": "percentage",
                    "ref1RM": "squat",
                    "setType": "straight",
                    "sets": 3,
                    "repsText": "5",
                },
                "instance": {
                    "pct": 0.7,
                    "kg": None,
                    "rpe": 7,
                    "note": "浏览器自动化已采纳",
                },
                "ref1RM": "squat",
                "pct": 0.7,
                "kg": None,
                "sets": 3,
                "reps": 5,
                "rpe": 7,
                "note": "浏览器自动化已采纳",
            }
        ],
    },
}
```

同时把 commit 返回计划更新为：

```python
"Monday": {
    "type": "腿日",
    "exercises": [
        {
            **WEEKLY_PLAN["Monday"]["exercises"][0],
            "id": "monday-squat-recovery",
            "sets": 3,
            "pct": 0.7,
            "rpe": 7,
            "note": "浏览器自动化已采纳",
        }
    ],
}
```

并在采纳后加一条 UI 断言：

```python
page.get_by_role("button", name="训练计划").click()
expect(page.get_by_text("浏览器自动化已采纳")).to_be_visible(timeout=5_000)
```

- [ ] **Step 2: 补 README 与 ARCHITECTURE**

README 只补一条能力描述，不写阶段性日志：

```markdown
- AI 教练支持“单日训练计划卡”采纳：确认后可直接把该日训练类型与动作写回训练计划
```

ARCHITECTURE 补一条 proposal kind 分流说明：

```markdown
- 计划 proposal 当前分为 `field_changes` 与 `day_plan_replace` 两类；commit 继续统一走 `/api/tools/plan/commit`
```

- [ ] **Step 3: 运行最终验证**

Run:

```powershell
node --test tests\adoptCard.test.js tests\weeklyPlanCommit.test.js tests\coachComposer.test.js
uv run pytest backend\tests\test_plan_tools.py backend\tests\test_chat_stream.py backend\tests\test_background_worker.py -q
npm run build
git diff --check
```

如本机需要浏览器级验证，再运行：

```powershell
uv run python -m playwright install chromium
uv run python "G:\AI Tools\codex-skills\webapp-testing\scripts\with_server.py" --server "npm run dev -- --host 127.0.0.1" --port 5173 -- uv run python tests\e2e\coach_browser_smoke.py
```

Expected：

- 单元测试、接口测试、构建全部通过
- 浏览器自动化可验证单日训练计划卡采纳后训练计划页展示同步更新

- [ ] **Step 4: 提交本子任务**

```powershell
git add tests/e2e/coach_browser_smoke.py README.md ARCHITECTURE.md
git commit -m "补充单日训练计划卡采纳验证与文档"
```

## 最终收口检查

- [ ] 确认旧版 `field_changes` proposal 仍能生成与 commit
- [ ] 确认新版 `day_plan_replace` proposal 能在前台流式、非流式和后台任务链路中返回
- [ ] 确认用户确认后训练计划页立即刷新
- [ ] 确认 `weekMeta` 等前端顶层元数据不因 commit 丢失
- [ ] 确认 README 与 ARCHITECTURE 已同步
