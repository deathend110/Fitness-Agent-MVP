# Task 1 V1.6 侧栏壳层严格对齐效果稿

- **估时**：45 分钟
- **依赖**：无
- **目标**：把应用左侧栏与主内容壳层严格对齐到 `训练计划页面效果图.html` 的侧栏结构、边框、留白和底部状态区表达，同时保证其余页面沿用同一壳层。
- **主要文件**：
  - `src/App.jsx`
  - `src/components/app-shell/*`
  - `src/index.css`
- **主要操作**：
  - 侧栏结构严格参考效果稿：品牌区、四个导航项、底部状态区。
  - 左侧栏在所有 tab 下统一样式。
  - 去掉效果稿侧栏底部的 `计划设置` 与 `AI 优化建议` 双按钮，仅保留状态提示区。
  - 主内容区边框、背景、左右留白与效果稿统一。
  - 不在本任务中处理训练计划页具体卡片结构。
- **完成信号**：
  - 左侧栏与效果稿样式基本一致。
  - 其他页面共享同一左侧栏。
  - 不再出现效果稿底部两个按钮。
- **验证方式**：
  - 逐个切换四个 tab，检查左侧栏始终一致。
  - 检查侧栏边框、间距和底部状态区是否与效果稿风格统一。

