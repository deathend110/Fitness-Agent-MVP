# Task 3 左侧栏与聊天记录列表严格对齐示意图

- **估时**：20 分钟
- **依赖**：Task 1、Task 2
- **目标**：按示意图重建左侧栏结构，让返回、创建对话、项目分组、聊天记录和设置入口严格落位。
- **主要文件**：
  - `src/tabs/CoachTab.jsx`
  - `src/components/CoachSidebar.jsx`（如需要新增）
  - `src/components/CoachThreadList.jsx`（如需要新增）
  - `src/components/CoachConversationPanel.jsx`
- **主要操作**：
  - 重建左侧栏的层级与留白，按示意图放置顶部返回、创建对话、项目区和底部设置。
  - 聊天记录列表按单列纵向排列，保留滚动浏览能力。
  - 不再保留旧页面里的提示词预览、日志摘要或其他辅助信息区作为主结构。
  - 不新增功能按钮，不改现有功能行为。
- **完成信号**：
  - 左侧栏与示意图的位置关系一致。
  - 聊天记录、项目入口、设置入口都在正确位置。
  - 页面没有残留旧的辅助信息区主导感。
- **验证方式**：
  - 打开空状态页面，确认左侧栏内容与示意图一致。
  - 列表较长时确认左侧栏支持滚动且布局不变形。
  - 检查页面里是否还存在旧的提示词预览或日志摘要主卡片。
